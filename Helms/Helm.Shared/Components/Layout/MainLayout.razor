@inherits LayoutComponentBase


<MudThemeProvider @ref="@_mudThemeProvider" @bind-IsDarkMode="@_isDarkMode" Theme="@_theme" />
@* <MudDialogProvider />
<MudPopoverProvider />
<MudSnackbarProvider /> *@

<MudLayout>
    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>


@*---------------------------------------------------------------------------------------------------- CODE *@

@code {
    private MudThemeProvider? _mudThemeProvider;
    private bool _isDarkMode;

    // ---------------------------------------------------------------------------------------------------- Main Functions

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            //_isDarkMode = await _mudThemeProvider.GetSystemPreference(); // Cannot set Dark Mode if Windows is not activated lol
            _isDarkMode = true;
            StateHasChanged();
        }
    }

    private readonly MudTheme _theme = new()
        {
            PaletteDark = new PaletteDark()
            {
                //Primary =   Colors.Pink.Darken1,
                //Secondary = Colors.Pink.Lighten1,
                //Tertiary =  Colors.Pink.Darken4,
                //Warning = Colors.Amber.Accent3,
                //Error =     Colors.Red.Accent3,
                //Info =      Colors.Blue.Accent3,
                //Success =   Colors.Green.Accent3
                //Defualt = Colors
                //Dark = Colors
            }
        };
}